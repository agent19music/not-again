import { Spacing } from '@/constants/theme';

export const TAB_BAR_ICON_SIZE = 24;
export const TAB_BAR_BOTTOM_OFFSET = Spacing.s16;
/** Pill height: vertical padding (top+bottom) around the tallest tab item (icon + its own padding). */
export const TAB_BAR_HEIGHT = Spacing.s12 * 2 + Spacing.s8 * 2 + TAB_BAR_ICON_SIZE;
